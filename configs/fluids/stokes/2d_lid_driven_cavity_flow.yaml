preset: stokes
parameters:
  nu: 1.0
domain:
  type: rectangle
  size: [1.0, 1.0]
  mesh_resolution: [32, 32]
inputs:
  velocity:
    source:
      type: none
      params: {}
boundary_conditions:
  velocity:
    $ref: boundary_field.vector.lid_driven_cavity_2d
output:
  $ref: output.template.velocity_pressure
  resolution: [64, 64]
  num_frames: 1
solver:
  $ref: solver.profile.steady_saddle_point
seed: 42
